namespace Assignment4
{
    /// <summary>
    /// An enum used to display priority options for the user.
    /// </summary>
    public enum PriorityType
    {
        VeryImportant, Important, Normal, LessImportant, NotImportant
    }
}
